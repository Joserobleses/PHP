{% extends 'base.html.twig' %}

{% block title %}Comprueba tu email{% endblock %}

{% block main %}
{{ parent() }}

	<h2>Email enviado</h2>
	<div class="alert alert-success p-3">
		Si existe una cuenta asociada al email indicado, recibirás en ella un enlace para actualizar el password.
	</div>
	<div class="p-3">
    <p>
 		Este enlace expirará en  {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.
    </p>
    <p>Si no recibe ningun email, consulta la carpeta de spam de tu gestor de correo o 
    <a href="{{ path('app_forgot_password_request') }}">intentalo de nuevo</a>.</p>
{% endblock %}
